# embed::Fn<RetType(ArgsType...)>::**operator bool**

| Destructor | Description
| --- | ---
| explicit operator bool() const noexcept; | Checks whether `*this` stores a callable function target, i.e. is not empty.